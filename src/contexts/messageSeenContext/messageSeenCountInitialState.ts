// messageSeenCountInitialState.ts
import { Store } from "../../@types/contexts/messageSeenCountContext/store.types";

const initialState: Store = {
    messageSeenCount: 0
};

export default initialState;
